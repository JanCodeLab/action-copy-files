{
  "name": "action-copy-files",
  "version": "1.0.0",
  "description": "GitHub Action to copy files with folder exclusion support",
  "main": "copy-files.js",
  "scripts": {
    "build": "ncc build copy-files.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "action",
    "copy",
    "files"
  ],
  "author": "JanCodeLab",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "glob": "^7.2.0",
    "fs.promises": "^0.1.2"
  }
}
